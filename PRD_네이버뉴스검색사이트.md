# PRD — 관심 키워드 뉴스 선별 사이트 (뉴스편식)

| 항목 | 내용 |
|---|---|
| 문서 버전 | v1.5 |
| 작성일 | 2026-08-18 |
| 작성자 | Halina Cho |
| 상태 | Draft |
| 프로젝트 성격 | AI 활용 실습 프로젝트 (프론트엔드 + 경량 API 프록시) |
| 데이터 소스 | 네이버 뉴스 검색 API (NAVER API HUB, `naverapihub.apigw.ntruss.com/search/v1/news`) |

---

## 1. 배경 및 목적

### 1.1 배경

이 프로젝트는 **AI 도구를 활용한 실습**을 목적으로 한다. 실제 서비스를 배포하기보다, 외부 API 연동 → 키 보안 처리 → UI 상태(로딩/빈 결과/에러) 설계까지 한 사이클을 직접 구현해보는 데 의의가 있다.

주제는 **AI, 로봇 등 최신 기술의 미래 동향**으로 한정한다. 사용자는 이 사이트에서 관련 키워드로 뉴스를 검색하고, 최신순/정확도순으로 정렬해 훑어볼 수 있다.

### 1.2 문제 정의

> **"최신 기술 동향을 파악하려면 여러 뉴스 사이트를 오가야 하고, 키워드 검색 결과를 원하는 기준으로 정렬해 보기 어렵다."**

### 1.3 비목표 (Non-goals)

- ⚠️ ~~사용자 계정 / 로그인~~ → v1.3에서 도입. Google 로그인으로 읽음 표시·저장함·키워드 설정을 기기 간 동기화한다
- ⚠️ ~~자체 DB 구축~~ → v1.3에서 도입(Supabase). 다만 **뉴스 아카이빙은 여전히 비목표다** — 기사 목록은 매 요청 네이버 API를 실시간 호출하고, DB에는 사용자별 개인 설정과 저장한 기사만 담는다
- ❌ 다국어 지원
- ❌ 상업적 운영 (네이버 오픈 API는 개인/비상업 개발자 등록 기준)
- ❌ 댓글, 스크랩, 공유 등 소셜 기능
- ❌ 뉴스 본문 전체 표시 (네이버 API는 요약/링크만 제공 — 원문은 외부 링크로 연결)

---

## 2. 목표

| 구분 | 목표 |
|---|---|
| **실습 목표** | 외부 API 연동, API 키 보안(.env.local + .gitignore), 상태별 UI(로딩/빈 결과/에러) 설계 경험 |
| **제품 목표** | 사용자가 직접 정한 관심 키워드만 골라 보는, 가볍고 빠른 뉴스 탐색 도구 완성 (기본 키워드는 AI·로봇 등 기술 트렌드로 제공하되 주제에 매이지 않는다) |
| **결과물 목표** | GitHub에 API 키 노출 없이 안전하게 코드 공개 가능한 상태로 완성 |

---

## 3. 타깃 사용자

**주 사용자 — 실습 진행자 본인 (Halina)**
- AI 활용 실습으로 직접 만들고 사용하는 1인 사용자 겸 개발자.
- 니즈: 만드는 과정에서 API 키 관리, 프록시 서버 구조, 에러 핸들링을 제대로 익히는 것.

**참고 사용자 — 기술 트렌드에 관심 있는 일반 방문자**
- "요즘 AI/로봇 쪽에 뭐가 뜨고 있나" 궁금해 가볍게 훑어보는 사람.
- 니즈: 키워드 칩 클릭 몇 번으로 관련 뉴스를 빠르게 훑어보고 싶다.

---

## 4. 정보 구조 및 화면 구성

```
┌───────────────────────────────────────────────────────┐
│  [로고: 뉴스편식]                [🔍 뉴스 검색창       ] │  ← 헤더 (고정)
├───────────────────────────────────────────────────────┤
│  인기 키워드                                            │
│  [ #AI ] [ #로봇 ] [ #휴머노이드 ] [ #자율주행 ]         │
│  [ #생성형AI ] [ #반도체 ] [ #양자컴퓨팅 ] [ #우주기술 ] │  ← 키워드 칩
├───────────────────────────────────────────────────────┤
│  검색결과 128건                     정렬: [정확도순 ▾]  │
│  ┌─────────────────────────────────────────────────┐  │
│  │ 📰 기사 제목 (검색어 하이라이트)                    │  │
│  │ 기사 요약 2줄 …                                    │  │
│  │ 출처 · 게재일시                          [원문보기]│  │
│  └─────────────────────────────────────────────────┘  │
│  ┌─────────────────────────────────────────────────┐  │
│  │ 📰 기사 카드 2 …                                   │  │
│  └─────────────────────────────────────────────────┘  │
│                     ⋮ (무한 스크롤 또는 페이지네이션)     │
└───────────────────────────────────────────────────────┘
```

### 4.1 초기 진입 화면

- 검색어 없이 최초 진입 시: 인기 키워드 칩 중 하나(기본값 "AI")가 자동 선택된 상태로 결과를 먼저 보여준다. 빈 화면으로 시작하지 않는다.

---

## 5. 기능 요구사항

### FR-00. API 키 보안 (P0) ⭐ 실습 핵심 요구사항

네이버 뉴스 검색 API는 **Client-Side에서 직접 호출할 수 없다** (CORS 미허용 + API 키가 헤더로 노출됨). 따라서 반드시 서버 사이드 프록시를 경유한다.

> **이관 안내 (2026-08 확인)**: 검색 API는 네이버 개발자센터(`openapi.naver.com`)에서 **NAVER API HUB**(네이버 클라우드 플랫폼)로 이관됐다.
> 신규 발급 키는 개발자센터 방식으로 인증되지 않으므로(401 `errorCode: 024`), 이 프로젝트는 HUB 엔드포인트를 기준으로 한다.

| ID | 요구사항 |
|---|---|
| FR-00-01 | Next.js(또는 동등한 프레임워크) API Route(`/api/news`)를 프록시로 두고, 브라우저는 이 내부 엔드포인트만 호출한다. |
| FR-00-02 | NAVER API HUB에서 발급받은 `API Key ID` / `API Key`는 `.env.local`에 저장한다. (`NAVER_API_KEY_ID`, `NAVER_API_KEY`) |
| FR-00-03 | `.env.local`은 **절대 커밋되지 않도록** 프로젝트 루트 `.gitignore`에 포함한다. |
| FR-00-04 | 저장소에는 `.env.local.example`(키 값은 비운 템플릿)만 커밋해, 협업자가 어떤 변수를 채워야 하는지 알 수 있게 한다. |
| FR-00-05 | API 키는 클라이언트 번들에 절대 포함되지 않는다. (`NEXT_PUBLIC_` 접두사 사용 금지) |
| FR-00-06 | 만약 실수로 키가 커밋된 이력이 있다면, `git rm --cached`로 추적 해제 후 NAVER API HUB 콘솔에서 **키를 재발급**한다. (히스토리에 남은 키는 무효화가 유일한 해결책) |

**5.0.1 `.gitignore` 예시**

```gitignore
# 환경변수 (절대 커밋 금지)
.env
.env.local
.env.*.local

# 의존성 / 빌드 산출물
node_modules/
.next/
dist/
out/

# 시스템 / 에디터
.DS_Store
*.log
.vscode/
```

**5.0.2 `.env.local` 예시 (실제 값은 각자 입력, 커밋 금지)**

```env
NAVER_API_KEY_ID=your_api_key_id_here
NAVER_API_KEY=your_api_key_here
```

**5.0.3 `.env.local.example` 예시 (이 파일만 커밋)**

```env
NAVER_API_KEY_ID=
NAVER_API_KEY=
```

**5.0.4 서버 프록시 흐름**

```
브라우저 → GET /api/news?query=AI&sort=sim&page=1
              ↓ (Next.js API Route, 서버에서만 실행)
         process.env.NAVER_API_KEY_ID / NAVER_API_KEY 사용
              ↓
GET https://naverapihub.apigw.ntruss.com/search/v1/news
    Headers: X-NCP-APIGW-API-KEY-ID, X-NCP-APIGW-API-KEY
              ↓
         응답을 그대로(혹은 가공해) 브라우저로 반환
```

> **주의**: `next.config.js`나 클라이언트 코드 어디에도 `process.env.NAVER_API_KEY`를 직접 참조하지 않는다. API Route(서버 전용 파일) 내부에서만 접근한다.

---

### FR-01. 인기 키워드 칩 (P0)

| ID | 요구사항 |
|---|---|
| FR-01-01 | 키워드를 칩 형태로 헤더 하단에 고정 노출한다. 칩 위에는 카테고리 탭을 두고, 선택된 카테고리의 칩만 보여준다. |
| FR-01-02 | 기본 카테고리·키워드 세트 (편집 가능 — `src/lib/constants.ts`의 `KEYWORD_CATEGORIES`) |
| | **기술트렌드**: `AI`, `생성형AI`, `휴머노이드`, `로봇`, `자율주행`, `반도체`, `양자컴퓨팅`, `우주기술` |
| | **주식**: `국내주식`, `미국주식`, `중국주식`, `코스피`, `나스닥`, `ETF`, `공모주` |
| | **크립토**: `비트코인`, `이더리움`, `리플`, `솔라나`, `알트코인`, `스테이블코인` |
| FR-01-03 | 칩 클릭 시 해당 키워드로 즉시 검색을 실행하고, 검색창에도 해당 키워드를 반영한다. |
| FR-01-04 | 현재 선택된 키워드 칩은 활성 스타일(색상 반전 또는 테두리 강조)로 구분한다. |
| FR-01-05 | 칩 목록은 가로 스크롤(모바일) 또는 줄바꿈(데스크톱)으로 자연스럽게 흐르도록 배치한다. |
| FR-01-06 | 사용자가 검색창에 직접 다른 키워드를 입력하면, 칩 선택은 해제된 상태로 전환한다. 이때 열려 있던 카테고리 탭은 그대로 유지한다. |
| FR-01-07 | 탭을 누르는 것만으로는 검색을 실행하지 않는다. 검색어가 어느 카테고리의 키워드로 바뀌면 해당 탭이 자동으로 열린다. |

---

### FR-02. 뉴스 검색 (P0)

| ID | 요구사항 |
|---|---|
| FR-02-01 | 검색창에 키워드 입력 후 Enter 또는 검색 버튼으로 조회한다. |
| FR-02-02 | 입력에 **300ms 디바운스**를 적용해 타이핑 중 과도한 API 호출을 방지한다. (즉시 검색 버튼 클릭 시에는 디바운스 무시) |
| FR-02-03 | 이전 요청은 `AbortController`로 취소해, 느린 응답이 이후 검색 결과를 덮어쓰는 경쟁 조건을 방지한다. |
| FR-02-04 | 검색어가 비어 있으면 검색을 실행하지 않고 안내 문구를 표시한다. |
| FR-02-05 | 검색 결과 상단에 총 검색 결과 수(`total`)를 표시한다. |

---

### FR-03. 기사 카드 리스트 (P0)

| ID | 요구사항 |
|---|---|
| FR-03-01 | 각 기사는 카드 형태로 렌더링한다: 제목 · 요약(description) · 출처(link의 도메인 또는 originallink) · 게재일시(pubDate) · [원문보기] 버튼 |
| FR-03-02 | 네이버 API 응답의 `title`/`description`에 포함된 `<b>` 태그(검색어 하이라이트)는 안전하게 처리해 굵은 글씨로만 렌더링한다. 그 외 HTML은 이스케이프해 XSS를 방지한다. |
| FR-03-03 | HTML 엔티티(`&quot;`, `&amp;` 등)를 사람이 읽을 수 있는 문자로 디코딩해 표시한다. |
| FR-03-04 | `[원문보기]`는 `originallink`(없으면 `link`)로 새 탭에서 연다. (`target="_blank" rel="noopener noreferrer"`) |
| FR-03-05 | `pubDate`는 상대 시간(예: "3시간 전") 또는 `YYYY.MM.DD HH:mm` 형식으로 가공해 표시한다. |
| FR-03-06 | 리스트는 반응형 그리드로 배치한다: 모바일 1열, 태블릿 2열, 데스크톱 2~3열. |
| FR-03-07 | 페이지네이션 또는 무한 스크롤로 추가 결과를 불러온다. 네이버 API는 `start`(1~1000) · `display`(최대 100)로 페이징한다. |
| FR-03-08 | 카드 로딩 중에는 스켈레톤 카드(shimmer) 6개를 표시한다. 최소 300ms 이상 노출해 깜빡임을 방지한다. |

---

### FR-04. 정렬 (P0)

| ID | 요구사항 |
|---|---|
| FR-04-01 | 정렬 옵션 2가지를 드롭다운으로 제공한다: **정확도순**(`sort=sim`, 기본값) / **최신순**(`sort=date`) |
| FR-04-02 | 정렬 변경 시 `start=1`로 초기화해 처음부터 다시 조회한다. |
| FR-04-03 | 현재 선택된 정렬 기준을 드롭다운에 명확히 표시한다. |
| FR-04-04 | 정렬 상태는 URL 쿼리스트링에 반영한다. (`?query=AI&sort=date&page=1`) 새로고침 시 상태가 보존된다. |

---

### FR-05. 결과 없음 화면 (P0)

| ID | 요구사항 |
|---|---|
| FR-05-01 | API 응답의 `total`이 0이면 결과 없음 화면을 표시한다. |
| FR-05-02 | 메시지: **"'{검색어}'에 대한 뉴스를 찾지 못했어요."** + 보조 설명: "다른 키워드로 검색하거나 인기 키워드를 선택해 보세요." |
| FR-05-03 | 결과 없음 화면 하단에 인기 키워드 칩 3~4개를 추천으로 재노출한다. |
| FR-05-04 | 텅 빈 텍스트만 두지 않고 일러스트/이모지를 함께 배치한다. |

---

### FR-06. 에러 화면 (P0)

| ID | 에러 유형 | 감지 조건 | 메시지 | 액션 |
|---|---|---|---|---|
| **E-01** | 네트워크 오프라인 | `navigator.onLine === false` 또는 fetch 실패 | "인터넷 연결이 끊겼어요. 연결을 확인한 뒤 다시 시도해 주세요." | [다시 시도] |
| **E-02** | 서버/프록시 오류 (5xx) | API Route 응답 HTTP 500대 | "뉴스를 불러오지 못했어요. 잠시 후 다시 시도해 주세요." | [다시 시도] |
| **E-03** | 인증 실패 (401) | 네이버 API가 401 반환 (키 오류/미설정) | "검색 서비스에 일시적인 문제가 있어요." (내부적으로는 환경변수 누락을 로그로 확인 — 사용자에게 키 관련 문구 노출 금지) | [다시 시도] |
| **E-04** | 요청 한도 초과 (429) | 네이버 일일 호출 한도(25,000회) 초과 | "요청이 많아 잠시 응답이 지연되고 있어요." | 안내 후 재시도 버튼 비활성화 유도 |
| **E-05** | 타임아웃 | 8초 무응답 | "응답이 너무 오래 걸려요." | [다시 시도] |
| **E-06** | 예상치 못한 오류 | React Error Boundary | "예상치 못한 문제가 발생했어요." | [새로고침] |

**5.6.1 공통 원칙**

| ID | 원칙 |
|---|---|
| FR-06-01 | 무엇이 문제인지 + 무엇을 하면 되는지를 함께 제시한다. |
| FR-06-02 | HTTP 상태 코드, 스택 트레이스, API 키 관련 정보는 사용자 화면에 절대 노출하지 않는다. |
| FR-06-03 | 자동 재시도는 지수 백오프(1s → 2s → 4s, 최대 3회)를 적용한다. |
| FR-06-04 | 이미 불러온 이전 페이지 결과가 있으면, 새 요청 실패 시에도 기존 목록은 유지한다. (전체 화면을 에러로 덮지 않음) |

---

### FR-07. 모바일 반응형 (P0)

| ID | 요구사항 |
|---|---|
| FR-07-01 | 브레이크포인트: ~640px(모바일) / 641~1024px(태블릿) / 1025px~(데스크톱) |
| FR-07-02 | 모바일에서 검색창은 헤더 전체 너비를 차지하며, 키워드 칩은 가로 스크롤된다. |
| FR-07-03 | 모바일에서 정렬 드롭다운은 하단 시트 또는 컴팩트 셀렉트로 축소한다. |
| FR-07-04 | 카드 리스트는 모바일 1열 → 태블릿 2열 → 데스크톱 2~3열로 전환한다. |
| FR-07-05 | 터치 타깃(칩, 버튼)은 최소 44×44px를 보장한다. |

---

## 6. API 명세 — 네이버 뉴스 검색 API

| 항목 | 내용 |
|---|---|
| 엔드포인트 | `https://naverapihub.apigw.ntruss.com/search/v1/news` (경로 끝에 `.json`을 붙이면 404) |
| 인증 | 헤더 `X-NCP-APIGW-API-KEY-ID`, `X-NCP-APIGW-API-KEY` (서버 사이드에서만 첨부) |
| 주요 파라미터 | `query`(필수) · `display`(10~100, 기본 10) · `start`(1~1000, 기본 1) · `sort`(`sim` 정확도순 \| `date` 최신순) |
| 호출 한도 | NAVER API HUB 콘솔의 상품/요금제 화면에서 확인한다. (이관으로 기존 개발자센터 기준 일 25,000회와 달라질 수 있음) |
| 응답 필드 | `lastBuildDate`, `total`, `start`, `display`, `items[]` (각 `title`, `originallink`, `link`, `description`, `pubDate`) |

**요청 예시 (서버 API Route 내부)**

```
GET https://naverapihub.apigw.ntruss.com/search/v1/news?query=AI&display=20&start=1&sort=sim
Headers:
  X-NCP-APIGW-API-KEY-ID: {서버 환경변수 NAVER_API_KEY_ID}
  X-NCP-APIGW-API-KEY: {서버 환경변수 NAVER_API_KEY}
```

> 응답 본문 구조(`lastBuildDate`·`total`·`start`·`display`·`items[]`)와 파라미터는 개발자센터 시절과 동일하므로, 파싱 코드는 그대로 쓸 수 있다.

> 네이버 뉴스 검색 API는 **최신 기사 전체를 실시간 색인하지 않을 수 있고**, 검색 결과가 언론사 API 제휴 범위에 한정될 수 있다는 점을 감안한다. (공식 문서 기준 특성이며 실습 중 결과가 예상보다 적을 수 있음을 염두에 둘 것)

---

## 7. 데이터 모델

```ts
interface NewsItem {
  title: string;            // <b> 태그 포함 원본 → 파싱 후 사용
  originallink: string;
  link: string;
  description: string;      // <b> 태그 포함 원본
  pubDate: string;           // RFC 822 형식
}

interface NewsSearchResponse {
  lastBuildDate: string;
  total: number;
  start: number;
  display: number;
  items: NewsItem[];
}

interface AppState {
  query: string;
  selectedKeywordChip: string | null;
  sort: "sim" | "date";
  page: number;               // display=20 기준 페이지 인덱스
  results: NewsItem[];
  total: number;
  status: "idle" | "loading" | "success" | "empty" | "error";
  error: { type: string; message: string } | null;
}
```

---

## 8. 기술 스택 (제안)

| 영역 | 선택 | 근거 |
|---|---|---|
| 프레임워크 | Next.js (App Router) | API Route로 서버 프록시를 같은 프로젝트 내에서 간단히 구성 가능 |
| 언어 | TypeScript | API 응답 타입 안정성 |
| 스타일 | Tailwind CSS | 반응형 그리드·칩 UI를 빠르게 구성 |
| 상태 관리 | React Query (TanStack Query) | 캐싱·재시도·로딩상태 관리를 라이브러리 레벨에서 처리 |
| 배포 | Vercel | 환경변수(Environment Variables)를 대시보드에서 안전하게 별도 관리 가능 |

> **배포 시 주의**: Vercel 등에 배포할 때는 `.env.local` 파일을 올리는 게 아니라, 호스팅 플랫폼의 **환경변수 설정 화면**에 `NAVER_API_KEY_ID`/`NAVER_API_KEY`를 동일하게 등록한다.

---

## 9. 엣지 케이스

| # | 상황 | 처리 방안 |
|---|---|---|
| EC-01 | 검색어에 특수문자/이모지 포함 | `encodeURIComponent`로 인코딩 후 요청 |
| EC-02 | `title`/`description`에 포함된 `<b>` 외 다른 HTML 태그 | 허용 태그(`<b>`)만 파싱하고 나머지는 이스케이프 (XSS 방지) |
| EC-03 | `originallink`가 빈 문자열 | `link`로 폴백 |
| EC-04 | 같은 키워드 칩을 연속 클릭 | 중복 요청 방지 (이미 선택된 상태면 재조회 생략) |
| EC-05 | `start` 값이 1000 초과 (네이버 API 상한) | "더 이상 불러올 결과가 없어요" 안내 후 다음 페이지 버튼 비활성화 |
| EC-06 | 일일 호출 한도 근접 | 클라이언트 캐시(React Query staleTime)로 동일 검색 재호출 최소화 |
| EC-07 | `.env.local` 미설정 상태로 로컬 실행 | 서버 콘솔에만 "환경변수가 설정되지 않았습니다" 로그 출력, 사용자 화면은 E-03과 동일 처리 |

---

## 10. 개발 마일스톤

| 단계 | 산출물 | 완료 기준 |
|---|---|---|
| **M0. 셋업 & 보안** | 프로젝트 스캐폴딩, `.gitignore`, `.env.local`, `.env.local.example`, 네이버 개발자센터 앱 등록 | `git status`에 `.env.local`이 뜨지 않음 확인 |
| **M1. API 프록시** | `/api/news` Route 구현, 네이버 API 연동 | 서버 로그로 정상 응답 확인, 클라이언트 번들에 키 미포함 확인 |
| **M2. 검색 + 정렬 + 카드 리스트** | FR-02, FR-03, FR-04 | 키워드 검색 → 카드 렌더 → 정렬 전환 정상 동작 |
| **M3. 인기 키워드 칩** | FR-01 | 칩 클릭 시 즉시 검색 반영 |
| **M4. 빈 결과 / 에러 화면** | FR-05, FR-06 | 존재하지 않는 검색어, 네트워크 차단 상태 수동 테스트 통과 |
| **M5. 반응형 & 폴리싱** | FR-07 | 모바일/태블릿/데스크톱 3개 뷰포트 수동 확인 |
| **M6. 배포** | Vercel 배포, 환경변수 대시보드 등록, README(설정 방법 포함) | 배포 후 실제 검색 정상 동작 |

---

## 11. README에 반드시 포함할 안내 (실습 기록용)

- 네이버 개발자센터(Application 등록) 링크와 발급받은 Client ID/Secret을 로컬 `.env.local`에 넣는 방법
- `.env.local.example` 복사 안내 (`cp .env.local.example .env.local`)
- 로컬 실행 명령어
- 배포 시 환경변수를 호스팅 플랫폼 대시보드에 별도 등록해야 한다는 안내

---

## 부록. 문서 개정 이력

| 버전 | 날짜 | 변경 내용 | 작성자 |
|---|---|---|---|
| v1.0 | 2026-08-18 | 최초 작성 | Halina Cho |
| v1.1 | 2026-08-18 | 검색 API의 NAVER API HUB 이관 반영 — 엔드포인트·인증 헤더·환경변수명(`NAVER_API_KEY_ID`/`NAVER_API_KEY`) 변경 | Halina Cho |
| v1.2 | 2026-08-18 | 키워드 칩을 카테고리(기술트렌드·주식·크립토) 구조로 변경 (FR-01) | Halina Cho |
| v1.3 | 2026-08-19 | Supabase 연동. Google 로그인 도입과 함께 읽음 표시·저장함(북마크)·키워드 직접 편집 추가. 비목표 2개 항목 해제 (1.3 참고) | Halina Cho |
| v1.3.1 | 2026-08-19 | 로그인 제공자를 카카오에서 Google로 변경 — Supabase의 카카오 제공자가 강제 요청하는 `account_email`·`profile_image` 동의항목이 카카오 비즈니스 앱 전환을 요구해 코드로 우회 불가 | Halina Cho |
| v1.4 | 2026-08-19 | 대시보드 페이지 삭제, 검색 화면을 루트(/)로 이동. /search 는 리다이렉트로 유지. 대시보드 전용 키워드 설정(dashboard_keywords)도 함께 제거 | Halina Cho |
| v1.5 | 2026-08-19 | 가칭 TechPulse 를 정식 명칭 **뉴스편식**으로 확정. 키워드를 사용자가 직접 정의하게 되면서 앱이 더 이상 특정 주제(AI·로봇)에 매이지 않으므로, 주제가 아니라 동작(골라 읽기)을 가리키는 이름으로 바꿨다. 문서 제목도 함께 수정 | Halina Cho |
